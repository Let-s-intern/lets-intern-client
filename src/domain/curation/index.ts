// Curation domain entry point
// Only export the main screen component for use in pages
// Internal hooks, types, data, and models should be imported directly when needed
export { default as CurationScreen } from './screen/CurationScreen';
