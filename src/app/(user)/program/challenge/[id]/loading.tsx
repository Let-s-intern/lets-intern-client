import LoadingContainer from '@/components/common/ui/loading/LoadingContainer';

export default function Loading() {
  return (
    <LoadingContainer className="mt-[20%]" text="정보를 불러오는 중입니다.." />
  );
}
