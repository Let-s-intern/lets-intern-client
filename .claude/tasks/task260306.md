# 멘토 마이페이지 v0.2 — Task 목록

> **브랜치**: `LC-2888-멘토-마이페이지-ver01`
> **기준일**: 2026-03-06
> **PRD**: `.claude/tasks/prd.md`
> **이전 작업**: `done/260306/task260305.md` (v0.1.2), `done/260306/기능구현현황_v0.1.1.md`

---

## 목표

백엔드에서 추가된 API 필드(nickname, introduction, profileImgUrl, sns, user-career의 field/position/department, FileType USER_PROFILE)를 활용하여:
1. 프로필 페이지의 저장 기능을 실제 API와 연동
2. 경력 CRUD를 user-career API와 연동
3. 프로필 이미지 업로드 구현
4. 와이어프레임과 현재 UI의 디자인 차이 보정

---

## 전체 의존성 그래프

```
Phase 1: API 스키마 확장 (병렬 시작 가능)
  #1 userSchema 확장 ─────┬──► #4 ProfilePage 저장 연동
  #3 fileType 확장 ───────┼──► #5 프로필 이미지 업로드
  #2 careerSchema 확장 ───┼──► #6 경력 CRUD API 연동
                          │
Phase 2: 프로필 API 연동  │
  #4 ─── #5 ─── #6       │  (직렬, #1/#2/#3 완료 후)
                          │
Phase 3: UI 와이어프레임 정합 (Phase 1/2와 병렬 가능)
  #7  WeeklySummary 진행률 카드
  #8  ChallengePeriodBar 디자인
  #9  WeeklyCalendar + FeedbackModal 헤더
  #10 FeedbackActions 버튼 + FeedbackEditor Markdown
```

---

## Phase 1: API 스키마 확장 (병렬 시작 가능)

### Task #1 — userSchema에 nickname, introduction, profileImgUrl, sns 필드 추가

| 항목 | 내용 |
|------|------|
| **우선순위** | 🔴 높음 |
| **상태** | 🔲 pending |
| **의존** | 없음 |
| **차단** | #4, #5 |

**현재 상태**: `src/api/user/user.ts`의 `userSchema`(line 205-229)에 해당 필드 없음. `PatchUserBody`(line 263-282)에도 없음.

**작업 내용**:

1. **userSchema 확장** (`src/api/user/user.ts:205-229`)
   ```ts
   // 기존 필드 뒤에 추가
   nickname: z.string().nullable(),
   introduction: z.string().nullable(),
   profileImgUrl: z.string().nullable(),
   sns: z.string().nullable(),
   ```

2. **PatchUserBody 타입 확장** (`src/api/user/user.ts:263-282`)
   ```ts
   // 기존 필드 뒤에 추가
   nickname?: string | null;
   introduction?: string | null;
   profileImgUrl?: string | null;
   sns?: string | null;
   ```

**수정 파일**:
- `src/api/user/user.ts`

---

### Task #2 — userCareerSchema에 field, position, department 필드 추가

| 항목 | 내용 |
|------|------|
| **우선순위** | 🔴 높음 |
| **상태** | 🔲 pending |
| **의존** | 없음 |
| **차단** | #6 |

**현재 상태**: `src/api/career/careerSchema.ts`의 `userCareerSchema`(line 26-35)에 company, job, employmentType, startDate, endDate만 존재.

**작업 내용**:

1. **userCareerSchema 확장** (`src/api/career/careerSchema.ts:26-35`)
   ```ts
   export const userCareerSchema = z.object({
     id: z.number().nullable().optional(),
     company: z.string(),
     job: z.string(),
     employmentType: z.string(),
     startDate: yearMonthSchema,
     endDate: yearMonthSchema.nullable().optional(),
     // 신규 필드
     field: z.string().nullable().optional(),       // 업무분야
     position: z.string().nullable().optional(),     // 직책
     department: z.string().nullable().optional(),   // 부서명
   });
   ```

2. **careerFormSchema도 동일 확장** (line 51-97)
   - field, position, department를 optional nullable로 추가

**수정 파일**:
- `src/api/career/careerSchema.ts`

---

### Task #3 — fileType enum에 USER_PROFILE 추가

| 항목 | 내용 |
|------|------|
| **우선순위** | 🔴 높음 |
| **상태** | 🔲 pending |
| **의존** | 없음 |
| **차단** | #5 |

**현재 상태**: `src/api/file.ts`의 `fileType` enum(line 6-18)에 USER_PROFILE 없음. Swagger에는 USER_PROFILE이 존재.

**작업 내용**:

1. `fileType` enum에 `'USER_PROFILE'` 추가
   ```ts
   export const fileType = z.enum([
     'BANNER_MAIN',
     'BANNER_PROGRAM',
     'BANNER_POPUP',
     'CHALLENGE',
     'LIVE',
     'VOD',
     'BLOG',
     'REPORT',
     'BLOG_BANNER',
     'CURATION_ITEM',
     'BANNER_MAIN_BOTTOM',
     'USER_PROFILE',  // 추가
   ]);
   ```

**수정 파일**:
- `src/api/file.ts`

---

## Phase 2: 프로필 API 연동 (#1, #2, #3 완료 후)

### Task #4 — ProfilePage 저장 로직 구현 (usePatchUser 연동)

| 항목 | 내용 |
|------|------|
| **우선순위** | 🔴 높음 |
| **상태** | 🔲 pending |
| **의존** | #1 |
| **차단** | #5 |

**현재 상태**: `src/domain/mentor/profile/ProfilePage.tsx`에서 저장 버튼이 `alert('프로필 저장 기능은 준비 중입니다')`로 구현됨. useUserQuery에서 name, email, phoneNum만 초기값으로 채움.

**작업 내용**:

1. **useUserQuery 초기값 확장** (`ProfilePage.tsx`)
   ```ts
   useEffect(() => {
     if (!user) return;
     setFormData((prev) => ({
       ...prev,
       name: user.name ?? '',
       nickname: user.nickname ?? '',
       email: user.email ?? '',
       phoneNum: user.phoneNum ?? '',
       sns: user.sns ?? '',
     }));
     setIntroduction(user.introduction ?? '');
   }, [user]);
   ```

2. **저장 로직 구현** (`ProfilePage.tsx`)
   ```ts
   const { mutate: patchUser, isPending } = usePatchUser(
     () => alert('프로필이 저장되었습니다.'),
     (error) => alert('저장에 실패했습니다: ' + error.message),
   );

   const handleSave = useCallback(() => {
     patchUser({
       name: formData.name,
       nickname: formData.nickname,
       phoneNum: formData.phoneNum,
       sns: formData.sns,
       email: formData.email,
       introduction,
       profileImgUrl: formData.profileImgUrl ?? null,
     });
   }, [formData, introduction, patchUser]);
   ```

3. **BasicInfoFormData 타입 확장** (`BasicInfo.tsx`)
   - `profileImgUrl: string` 필드 추가

**수정 파일**:
- `src/domain/mentor/profile/ProfilePage.tsx`
- `src/domain/mentor/profile/BasicInfo.tsx` (FormData 타입에 profileImgUrl 추가)

---

### Task #5 — 프로필 이미지 업로드 구현

| 항목 | 내용 |
|------|------|
| **우선순위** | 🟡 중간 |
| **상태** | 🔲 pending |
| **의존** | #1, #3, #4 |
| **차단** | 없음 |

**현재 상태**: `BasicInfo.tsx`에 프로필 이미지 placeholder(회색 X 표시)가 있으나 업로드 기능 없음.

**와이어프레임 기준** (`profile.png`):
- 좌측 큰 사각형 영역
- 업로드 아이콘(↑) + 삭제 아이콘(🗑) 버튼
- "권장 600px (5MB 이하)" 안내 텍스트

**작업 내용**:

1. **이미지 업로드 로직** (`BasicInfo.tsx`)
   ```ts
   import { uploadFile } from '@/api/file';

   const handleImageUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
     const file = e.target.files?.[0];
     if (!file) return;

     // 5MB 제한
     if (file.size > 5 * 1024 * 1024) {
       alert('파일 크기는 5MB 이하여야 합니다.');
       return;
     }

     const fileUrl = await uploadFile({ file, type: 'USER_PROFILE' });
     onChange({ ...formData, profileImgUrl: fileUrl });
   };
   ```

2. **이미지 미리보기** — profileImgUrl이 있으면 `<img>` 표시, 없으면 placeholder
3. **삭제 버튼** — profileImgUrl을 빈 문자열로 설정
4. **hidden input** — `<input type="file" accept="image/*" className="hidden">` + ref 연결

**수정 파일**:
- `src/domain/mentor/profile/BasicInfo.tsx`

---

### Task #6 — 경력 CRUD API 연동 (CareerSection)

| 항목 | 내용 |
|------|------|
| **우선순위** | 🟡 중간 |
| **상태** | 🔲 pending |
| **의존** | #2 |
| **차단** | 없음 |

**현재 상태**: `CareerSection.tsx`가 로컬 useState로 careers 배열을 관리. API 연동 없음. 필드: company, field, position, department, startDate, endDate (text input).

**기존 API 훅** (`src/api/career/career.ts`):
- `useGetUserCareerQuery(pageable, sortable)` — 목록 조회
- `usePostUserCareerMutation()` — 생성 (multipart/form-data)
- `usePatchUserCareerMutation()` — 수정 (multipart/form-data)
- `useDeleteUserCareerMutation()` — 삭제

**작업 내용**:

1. **경력 목록 조회** (`ProfilePage.tsx`)
   ```ts
   const { data: careerData } = useGetUserCareerQuery({ page: 0, size: 100 });
   const serverCareers = careerData?.userCareers ?? [];
   ```
   - 서버 경력 데이터를 CareerSection 초기값으로 전달

2. **CareerSection 리팩터링** (`CareerSection.tsx`)
   - 기존 Career 인터페이스를 API 스키마와 정렬:
     ```ts
     interface Career {
       id?: number | null;    // 서버 경력은 id 있음, 신규는 null
       company: string;
       job: string;           // 기존 field → job으로 매핑 (API 필드명)
       employmentType: string;
       field: string | null;       // 업무분야 (신규)
       position: string | null;    // 직책 (신규)
       department: string | null;  // 부서명 (신규)
       startDate: string;
       endDate: string | null;
     }
     ```

3. **경력 추가** — 새 경력 카드 추가 시 `usePostUserCareerMutation` 호출
   - multipart/form-data: `requestDto` (JSON blob) + `verificationFile` (선택)

4. **경력 수정** — 필드 변경 시 blur/save에서 `usePatchUserCareerMutation` 호출

5. **경력 삭제** — X 버튼 시 `useDeleteUserCareerMutation` 호출

6. **와이어프레임 정합**:
   - 업무분야, 직책: 현재 text input → 드롭다운으로 변경 (선택지 목록은 확인 필요, 우선 text 유지)
   - 입사일, 퇴사일: 현재 text placeholder → YYYY.MM 형식 input (type="month" 또는 커스텀)

**수정 파일**:
- `src/domain/mentor/profile/ProfilePage.tsx`
- `src/domain/mentor/profile/CareerSection.tsx`

---

## Phase 3: UI 와이어프레임 정합 (Phase 1/2와 병렬 가능)

### Task #7 — WeeklySummary 진행률 카드 추가

| 항목 | 내용 |
|------|------|
| **우선순위** | 🟡 중간 |
| **상태** | 🔲 pending |
| **의존** | 없음 |
| **차단** | 없음 |

**현재 상태**: `WeeklySummary.tsx`에 3개 카드(이번주 전체, 오늘 마감, 미완료). 와이어프레임에는 4개 카드(+ 진행률).

**와이어프레임 기준** (`program_schedule.png`):
- 4번째 카드: "진행률" + 66% + 보라색 프로그레스 바

**작업 내용**:

1. **WeeklySummary props 확장**
   ```ts
   interface WeeklySummaryProps {
     totalCount: number;
     todayDueCount: number;
     incompleteCount: number;
     progressRate: number;  // 0-100
   }
   ```

2. **진행률 카드 UI**
   - 라벨: "진행률"
   - 값: `{progressRate}%` (큰 숫자)
   - 하단: 프로그레스 바 (bg-purple-500)

3. **SchedulePage에서 progressRate 계산**
   ```ts
   const progressRate = useMemo(() => {
     const total = allBars.reduce((sum, b) => sum + b.submittedCount + b.notSubmittedCount, 0);
     const completed = allBars.reduce((sum, b) => sum + b.completedCount, 0);
     return total > 0 ? Math.round((completed / total) * 100) : 0;
   }, [allBars]);
   ```

4. **그리드를 3열 → 4열로 변경**: `grid-cols-3` → `grid-cols-4`

**수정 파일**:
- `src/domain/mentor/schedule/WeeklySummary.tsx`
- `src/domain/mentor/schedule/SchedulePage.tsx` (progressRate 계산 + prop 전달)

---

### Task #8 — ChallengePeriodBar 디자인 와이어프레임 정합

| 항목 | 내용 |
|------|------|
| **우선순위** | 🟡 중간 |
| **상태** | 🔲 pending |
| **의존** | 없음 |
| **차단** | 없음 |

**현재 상태**: `ChallengePeriodBar.tsx`는 단일 베이지 바에 `[서면]` 태그 + 챌린지명 + 미제출/제출/시작전/진행중/완료 텍스트.

**와이어프레임 기준** (`program_schedule.png`):
- 상단: 문서 아이콘 + `[N차 피드백]` + `시작 전 N` (빨간) · `진행 중 N` · `완료 N` + 가로 컬러 라인
- 중단: 챌린지명 태그 (주황/파랑 컬러 배지 — 챌린지별 색상 구분)
- 우측: `미제출 N · 제출 N`

**작업 내용**:

1. **바 레이아웃 변경**
   ```
   ┌──────────────────────────────────────────┐
   │ 📄 [N차 피드백]  시작 전 1 · 진행 중 2 · 완료 12  ───── (가로 컬러 라인)
   │ [포트폴리오 챌린지 3기]          미제출 3 · 제출 12
   └──────────────────────────────────────────┘
   ```

2. **챌린지별 색상 구분**
   - 챌린지 인덱스에 따라 컬러 팔레트 순환 (주황, 파랑, 초록, 보라 등)
   - `PeriodBarData`에 `colorIndex` 또는 `challengeColor` 추가 필요

3. **가로 컬러 라인** — 바 상단 border-top 색상을 챌린지 색상으로

**수정 파일**:
- `src/domain/mentor/schedule/ChallengePeriodBar.tsx`
- `src/domain/mentor/schedule/SchedulePage.tsx` (챌린지별 컬러 인덱스 전달)

---

### Task #9 — WeeklyCalendar 일요일 빨간색 + FeedbackModal 헤더 개선

| 항목 | 내용 |
|------|------|
| **우선순위** | 🟢 낮음 |
| **상태** | 🔲 pending |
| **의존** | 없음 |
| **차단** | 없음 |

**작업 내용**:

#### 9-1. WeeklyCalendar 일요일 빨간색
- `WeeklyCalendar.tsx`의 DAY_LABELS 배열 마지막 "SUN"을 빨간색으로 표시
- 와이어프레임: 일요일 요일 + 날짜가 모두 빨간색
  ```tsx
  const isSunday = i === 6;
  // 적용: text-red-500 (요일 레이블 + 날짜)
  ```

#### 9-2. FeedbackModal 헤더 개선
- `FeedbackModal.tsx` 상단에 정보 바 추가:
  - 좌측: `{챌린지명} · {N}차 피드백`
  - 중앙: `총 {N}명` + `시작 전 {N} · 진행 중 {N} · 완료 {N}`
  - 우측: `피드백 가이드 라인` 링크 + X 닫기
- 와이어프레임 (`Feedback.png`) 상단 바 참조

#### 9-3. MenteeInfo 이전/다음 멘티 네비게이션
- `< 이전 멘티` / `다음 멘티 >` 버튼 추가
- 현재 선택된 attendanceId의 인덱스 기반 prev/next 계산
- FeedbackModal에서 attendanceList를 MenteeInfo에 전달하거나, 네비게이션 콜백 전달

**수정 파일**:
- `src/domain/mentor/schedule/WeeklyCalendar.tsx`
- `src/domain/mentor/feedback/FeedbackModal.tsx`
- `src/domain/mentor/feedback/MenteeInfo.tsx`

---

### Task #10 — FeedbackActions 버튼 스타일 + FeedbackEditor Markdown 서식

| 항목 | 내용 |
|------|------|
| **우선순위** | 🟢 낮음 |
| **상태** | 🔲 pending |
| **의존** | 없음 |
| **차단** | 없음 |

**작업 내용**:

#### 10-1. FeedbackActions 버튼 스타일 변경
- 와이어프레임 (`Feedback.png`) 기준:
  - 임시저장: outlined 스타일 (`border border-primary text-primary`)
  - 피드백 제출: solid primary (`bg-primary text-white`)
- 현재: 둘 다 `border-gray-300 text-gray-700` 동일 스타일

#### 10-2. FeedbackEditor Markdown 서식 지원 강화
- 현재 `FeedbackEditor.tsx`는 `EditorApp`을 사용 중 (Lexical)
- EditorApp에 이미 서식 플러그인이 포함되어 있을 가능성 높음 → 확인 필요
- 부족하면 MarkdownShortcutPlugin 등 추가

**수정 파일**:
- `src/domain/mentor/feedback/FeedbackActions.tsx`
- `src/domain/mentor/feedback/FeedbackEditor.tsx` (필요시)

---

## 수정/생성 파일 요약

### 수정 (총 12개 파일)

| 파일 | Task |
|------|------|
| `src/api/user/user.ts` | #1 |
| `src/api/career/careerSchema.ts` | #2 |
| `src/api/file.ts` | #3 |
| `src/domain/mentor/profile/ProfilePage.tsx` | #4, #6 |
| `src/domain/mentor/profile/BasicInfo.tsx` | #4, #5 |
| `src/domain/mentor/profile/CareerSection.tsx` | #6 |
| `src/domain/mentor/schedule/WeeklySummary.tsx` | #7 |
| `src/domain/mentor/schedule/SchedulePage.tsx` | #7, #8 |
| `src/domain/mentor/schedule/ChallengePeriodBar.tsx` | #8 |
| `src/domain/mentor/schedule/WeeklyCalendar.tsx` | #9 |
| `src/domain/mentor/feedback/FeedbackModal.tsx` | #9 |
| `src/domain/mentor/feedback/MenteeInfo.tsx` | #9 |
| `src/domain/mentor/feedback/FeedbackActions.tsx` | #10 |
| `src/domain/mentor/feedback/FeedbackEditor.tsx` | #10 |

### 신규 파일: 없음

---

## 작업 순서 (권장)

```
병렬 A (API 스키마):   #1 → #2 → #3  (독립, 즉시 시작)

병렬 B (프로필 연동):   #4 → #5 → #6  (#1/#2/#3 완료 후)

병렬 C (UI 정합):      #7, #8, #9, #10  (독립, 즉시 시작 가능)
```

### 추천 실행 순서

```
시간 →

Phase 1:  #1 + #2 + #3 (병렬, 각 5분)
          ↓
Phase 2:  #4 (저장 연동) → #5 (이미지) → #6 (경력)
          ↓
Phase 3:  #7 + #8 + #9 + #10 (병렬, UI 작업)
          ↓
검증:     npx tsc --noEmit + 수동 테스트
```

---

## 완료 기준

- [ ] #1: `GET /api/v1/user` 응답에서 nickname, introduction, profileImgUrl, sns 파싱 정상
- [ ] #2: user-career 조회 시 field, position, department 포함
- [ ] #3: `uploadFile({ type: 'USER_PROFILE' })` 타입 에러 없음
- [ ] #4: 프로필 저장 버튼 → PATCH API 호출 → 성공 알림
- [ ] #5: 프로필 이미지 선택 → S3 업로드 → 미리보기 표시
- [ ] #6: 경력 추가/수정/삭제가 서버에 반영
- [ ] #7: 주간 요약에 진행률 카드(4번째) 표시
- [ ] #8: 기간 바가 와이어프레임 디자인과 일치
- [ ] #9: 일요일 빨간색 + 모달 헤더에 총 인원/상태 요약 표시
- [ ] #10: 피드백 제출 버튼이 primary 색상, 에디터 서식 지원
- [ ] 전체: `npx tsc --noEmit` 통과
