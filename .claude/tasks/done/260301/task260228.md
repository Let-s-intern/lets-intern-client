## 관련 파일

### 수정 대상
- `src/domain/curation/hero/CurationHero.tsx` — Hero 모바일 패딩 고정값 수정 (Task 1)
- `src/domain/curation/flow/MobileRecommendationCard.tsx` — 데스크톱 카드 구조에 맞게 전면 재작성 (Task 2)
- `src/domain/curation/flow/ResultSection.tsx` — 모바일 카드에 `showExtraButton` / `onExtraClick` props 전달 (Task 2)
- `src/domain/curation/challenge-comparison/ChallengeCompareSection.tsx` — 모바일 레이아웃 분기 + 플로팅 버튼 + MobileCompareView 연결 (Task 3, 4)

### 신규 생성
- `src/domain/curation/challenge-comparison/MobileChallengeCard.tsx` — 모바일 가로 배치 챌린지 카드 (Task 3)
- `src/domain/curation/challenge-comparison/MobileCompareView.tsx` — 모바일 전체화면 비교 결과 뷰 (Task 4)

### 참고 패턴
- `src/domain/challenge/MobileReviewModal.tsx` — 전체화면 모바일 오버레이 패턴
- `src/common/button/ApplyCTA.tsx` — 플로팅 하단 버튼 패턴 (MobileCTA)
- `src/common/header/BackHeader.tsx` — 뒤로가기 헤더
- `src/domain/curation/flow/DesktopRecommendationCard.tsx` — 모바일 카드 기준 구조
- `src/domain/curation/challenge-comparison/ChallengeCard.tsx` — CARD_COLORS 상수, 기존 카드 구조
- `src/domain/curation/challenge-comparison/CompareResultCard.tsx` — InfoRow 테이블 행 패턴

---

## 작업

- [x] 1.0 Hero 모바일 반응형 패딩 수정
    - [x] 1.1 `CurationHero.tsx` `px-[120px]` → `px-6 md:px-10 lg:px-[120px]`

- [x] 2.0 MobileRecommendationCard 스타일 DesktopRecommendationCard 기준으로 맞추기
    - [x] 2.1 `MobileRecommendationCard.tsx` 구조를 DesktopRecommendationCard와 동일하게 재작성 (`w-full flex-col`)
    - [x] 2.2 `showExtraButton?: boolean`, `onExtraClick?: () => void` props 추가
    - [x] 2.3 `ResultSection.tsx`에서 모바일 카드에도 해당 props 전달

- [x] 3.0 모바일 챌린지 비교 — 세로 리스트 + 플로팅 버튼
    - [x] 3.1 `MobileChallengeCard.tsx` 신규 생성 (가로 배치: 썸네일 좌 + 텍스트/버튼 우)
    - [x] 3.2 `ChallengeCompareSection.tsx` 카드 그리드에 `hidden md:flex` / `md:hidden` 분기 추가
    - [x] 3.3 기존 데스크톱 하단 비교 버튼 블록에 `hidden md:flex` 적용
    - [x] 3.4 모바일 전용 플로팅 비교하기 버튼 추가 (`fixed bottom-0 ... md:hidden`)

- [x] 4.0 모바일 비교 결과 전체화면 뷰 (MobileCompareView)
    - [x] 4.1 `MobileCompareView.tsx` 신규 생성 (`fixed inset-x-0 top-0 z-50 h-screen` 전체화면)
    - [x] 4.2 sticky 헤더 (뒤로가기 버튼 + "비교함" 타이틀)
    - [x] 4.3 동적 제목 ("자소서 vs 마케팅" 등)
    - [x] 4.4 프로그램 썸네일 + 바로가기 버튼 — 좌우 2분할
    - [x] 4.5 비교 항목 테이블 (라벨 행 + 2분할 값 행)
    - [x] 4.6 `ChallengeCompareSection.tsx`에 `isMobileViewOpen` 상태 추가 및 MobileCompareView 연결

---

## PRD

`.claude/tasks/todo/prd.md` 참조
