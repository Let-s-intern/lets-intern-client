name: 어드민 검사

# apps/admin 또는 공용 패키지·모노레포 루트 설정 변경 시만 실행.
# 5단계 검사 직렬 실행 (E2E 없음 — web 한정).
on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'apps/admin/**'
      - 'packages/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'turbo.json'
      - 'package.json'
      - '.prettierrc.mjs'
      - '.github/workflows/build-admin.yml'
  push:
    branches: [main]
    paths:
      - 'apps/admin/**'
      - 'packages/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'turbo.json'
      - 'package.json'

concurrency:
  group: build-admin-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: admin 5단계 검사
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile

      - name: '1) Prettier check (admin 한정)'
        continue-on-error: true
        run: pnpm exec prettier --check "apps/admin/**/*.{ts,tsx,js,jsx,json,md}"

      - name: '2) ESLint (admin)'
        continue-on-error: true
        run: pnpm --filter @letscareer/admin lint

      - name: '3) Vitest (admin)'
        run: pnpm --filter @letscareer/admin test

      - name: '4) Coverage (V8, threshold 미설정)'
        continue-on-error: true
        run: pnpm --filter @letscareer/admin test:coverage

      - name: 'Upload admin coverage'
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: admin-coverage-${{ github.sha }}
          path: apps/admin/coverage
          if-no-files-found: ignore
          retention-days: 7

      - name: '5) Build (admin)'
        run: pnpm --filter @letscareer/admin build
        env:
          VITE_SERVER_API: ${{ secrets.VITE_SERVER_API || 'https://api.example.com/v1' }}
          VITE_SERVER_API_V2: ${{ secrets.VITE_SERVER_API_V2 || 'https://api.example.com/v2' }}
          VITE_SERVER_API_V3: ${{ secrets.VITE_SERVER_API_V3 || 'https://api.example.com/v3' }}
          VITE_API_BASE_PATH: ${{ secrets.VITE_API_BASE_PATH || 'https://api.example.com' }}
